---
const { href } = Astro.props;
---

<span class="link-container">
	<a {href} class="styled-link">
		<span><slot /></span>
	</a>
</span>

<style>
	.link-container {
		display: inline-block;
		align-items: center;
		justify-content: center;
		position: relative;
	}

	.styled-link {
		cursor: pointer;
		position: relative;
		white-space: nowrap;
		text-decoration: none;
		padding-bottom: 0.25rem;
	}

	.styled-link:focus-visible {
		outline: 2px solid var(--color-sea-blue);
		outline-offset: 4px;
		border-radius: 2px;
	}

	.styled-link::before {
		content: "";
		transform-origin: 50% 100%;
		background: var(--color-sea-blue);
		transition:
			clip-path var(--dur-base) var(--ease-hover),
			transform var(--dur-base) var(--ease-out);
		position: absolute;
		width: 100%;
		height: 1px;
		top: 95%;
		left: 0;
		pointer-events: none;
		clip-path: polygon(
			0% 0%,
			0% 100%,
			0 100%,
			0 0,
			100% 0,
			100% 100%,
			0 100%,
			0 100%,
			100% 100%,
			100% 0%
		);
	}

	.styled-link:is(:hover, :focus-visible)::before {
		transform: translate3d(0, 2px, 0) scale3d(1, 2, 1);
		clip-path: polygon(
			0% 0%,
			0% 100%,
			100% 100%,
			50% 0,
			50% 0,
			50% 100%,
			50% 100%,
			0 100%,
			100% 100%,
			100% 0%
		);
	}

	.styled-link span {
		display: inline-block;
		transition:
			transform var(--dur-base) var(--ease-out),
			color var(--dur-quick) var(--ease-hover);
		color: var(--color-bright-crimson);
	}

	.styled-link:is(:hover, :focus-visible) span {
		transform: translate3d(0, -2px, 0);
		color: var(--color-crimson);
	}

	.styled-link:active span {
		transform: translate3d(0, -1px, 0) scale(0.98);
		transition-duration: var(--dur-micro);
	}
</style>
